# docs/handoff/

每個 `.md` = **一個尚未完成的待做功能/修復**。後續 session 挑一個檔案專心做完，**做完直接刪掉該 md**。
寫作原則同全域 doc 偏好（token-dense、狀態旗標、pointer 引用），主要讀者是下一個 session 的 Claude。
已完成的設計/踩坑請寫進對應 source 的註解或 `docs/enhanced-addon.md`，不要留在這裡。
