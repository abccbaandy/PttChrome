node_modules
**/*.concat.*

# Yarn v4（node-modules linker；cache 走全域）
.yarn/*
!.yarn/patches
.pnp.*

# yarn 專案，勿用 npm
package-lock.json

# Playwright E2E
test-results/
playwright-report/
tests/e2e/__screenshots__/
# 录制 cassette 用的 PTT 凭证（绝不入 repo）
tests/e2e/.ptt-creds.json

# Firebase emulator（yarn test:integration）
firebase-debug.log
firestore-debug.log
ui-debug.log

js/constants_local.js
dist/*
!dist/.gitkeep

.idea/
3rd_script/

# Claude Code hook 的旗標檔（本 session 是否啟動過 dev server；見 .claude/settings.json）
.claude/.dev-server-running
.claude/settings.local.json